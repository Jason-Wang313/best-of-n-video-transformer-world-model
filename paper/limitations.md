# Limitations

The environment is synthetic. The dynamics are intentionally simple. The learned video transformer is small and CPU-friendly. The repair ladder is tuned to the diagnostics available in this scaffold, and broader use would require new held-out execution labels.

The evidence should be read as a controlled warning and an audit method, not as a statement about every video model, every planner, or every deployment domain.
